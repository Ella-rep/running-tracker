<header class="app-header">
  <div class="header-left">
    <div class="logo">RUN<span>/</span>TRACKER</div>
    {% include 'components/_theme_toggle.html.twig' with { with_onclick: true } %}
  </div>
  <button
    class="mobile-nav-toggle"
    id="mobile-nav-toggle"
    type="button"
    aria-label="Ouvrir le menu"
    aria-expanded="false"
    aria-controls="app-nav"
  >
    <span class="mobile-nav-toggle-bar"></span>
    <span class="mobile-nav-toggle-bar"></span>
    <span class="mobile-nav-toggle-bar"></span>
  </button>
  <nav id="app-nav" class="app-nav" aria-label="Navigation principale">
    <a href="{{ path('app_home') }}" class="nav-link{{ current_page == 'home' ? ' active' : '' }}">Accueil</a>
    <a href="{{ path('app_dashboard') }}" class="nav-link{{ current_page == 'dashboard' ? ' active' : '' }}">Dashboard</a>
    <a href="{{ path('app_plans') }}" class="nav-link{{ current_page == 'plans' ? ' active' : '' }}">Plans</a>
    <a href="{{ path('app_log') }}" class="nav-link{{ current_page == 'log' ? ' active' : '' }}">Log</a>
    <a href="{{ path('app_courses') }}" class="nav-link{{ current_page == 'courses' ? ' active' : '' }}">Courses</a>
    <a href="{{ path('app_contact') }}" class="nav-link{{ current_page == 'contact' ? ' active' : '' }}">Contact</a>
    {% if is_granted('ROLE_ADMIN') %}
      <a
        id="admin-nav-link"
        href="{{ path('app_admin_users') }}"
        class="nav-link{{ current_page == 'admin_users' ? ' active' : '' }}"
      >Admin</a>
    {% endif %}
  </nav>
  <div class="user-badge">
    <span class="user-greeting">Bienvenue,</span>
    <strong id="current-username" class="current-username">{{ username ?: 'inconnu' }}</strong>
    <button class="logout-btn" type="button" onclick="logout()" title="Déconnexion" aria-label="Déconnexion">
      <span class="logout-icon" aria-hidden="true">⏻</span>
    </button>
  </div>
</header>

{% set announcement = active_admin_announcement() %}
{% if announcement %}
  {% set level = announcement.level|default('info') %}
  {% set bs_level = level %}
  {% set level_icon = 'ℹ' %}
  {% if level == 'critical' %}
    {% set bs_level = 'danger' %}
  {% endif %}
  {% if bs_level == 'success' %}
    {% set level_icon = '✔' %}
  {% elseif bs_level == 'warning' %}
    {% set level_icon = '⚠' %}
  {% elseif bs_level == 'danger' %}
    {% set level_icon = '⛔' %}
  {% endif %}
  <output
    class="global-announcement alert alert-{{ bs_level }} alert-dismissible fade show"
    data-announcement
    data-announcement-signature="{{ announcement.signature|default('') }}"
    aria-live="polite"
    role="alert"
  >
    <div class="global-announcement-inner">
      <span class="global-announcement-icon" aria-hidden="true">{{ level_icon }}</span>
      <div class="global-announcement-content">
        <span class="global-announcement-label">{{ announcement.title|default('Annonce') }}</span>
        <span class="global-announcement-message">{{ announcement.message|e|nl2br }}</span>
        {% if announcement.endsAt %}
          <span class="global-announcement-expiry">Jusqu'au {{ announcement.endsAt }}</span>
        {% endif %}
      </div>
      <button type="button" class="global-announcement-close" data-announcement-dismiss aria-label="Fermer l'annonce">×</button>
    </div>
  </output>
{% endif %}
